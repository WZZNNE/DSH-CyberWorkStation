// Section 01 — the DSH Launcher, one figure per page (13 pages).
export default {
  id: 'launcher',
  num: '01',
  kicker: '',
  title: 'DSH 启动器：13 个页面',
  intro: `
启动器是一个独立的小程序：双击 \`DSH启动器.exe\`，它以无地址栏的应用窗口打开 \`http://127.0.0.1:3090\`。只监听本机，页面自带访问令牌。左下角「中 → EN」切换语言。

截图时本体处于 OFFLINE 状态，所以需要 dsh 在线才能读取的页面（模型参数、凭据中心、记忆与上下文）显示的是占位提示；在线后能看到什么，在各图说明里补充。
`,
  blocks: [
    { type: 'figure', img: '01-launcher-dashboard', fig: '01', title: '仪表盘', sub: '',
      what: `
顶部横幅显示运行状态（OFFLINE / RUNNING）；四张卡片是本体版本、Node 版本、默认模型（旁边直接链到凭据中心去改）和「打开 WEB UI」。「文件夹」一栏输入绝对路径就能新建工作区；下面四张快捷卡分别打开本体仓库、\`~/.dsh\` 主目录、插件目录和会话日志目录。中间的控制台实时滚动本体的输出。右下角是「一键启动」和「退出」。
`,
      did: `
- 本体已构建时启动约 1.5 秒；未构建时回退到源码方式启动。
- 退出只停 dsh 自己的进程，不会误杀其它终端或程序；重复点击不会启动第二份。
- 「新建工作区」输入路径即建，刷新 dsh 页面就能选用（本体自带的「+」按钮对未分组会话是无效的）。
`,
      beyond: `
「一键启动」在皮肤里带电流特效，RUNNING 状态是翻牌动画；\`#deck\` 这类地址深链可以直接打开对应页面。
`},
    { type: 'figure', img: '02-launcher-plugins', fig: '02', title: '插件管理', sub: '',
      what: `
上方输入 npm 包名或 \`link:\` 本地路径安装；表格列出 web profile 里的每个插件：版本、来源（npm / link）、是否作为 bundle 层挂载，以及「卸载」。社区插件和套件插件混排在同一张表里。表格下方是「插件市场」（搜 npm 直接安装）和「内置组合行」（本体自带的一百多行只读挂载清单）。
`,
      did: `
- 安装、卸载、市场安装、更新都走本体自己的插件命令，和命令行操作完全等价。
- 两个被 profile 补丁引用的插件（凭据钥匙串、LAN 防线）卸载前会被拦住，并给出迁移步骤，避免下次启动加载不存在的模块。
- 插件市场按 dsh 生态过滤 npm 结果；皮肤包不会混进插件列表（另有皮肤市场）。
`,
      beyond: `
8 个社区插件就是从这一页装的。\`dsh-context\` 需要 0.40 以上版本，自检会点名过旧的版本。
`},
    { type: 'figure', img: '03-launcher-skills', fig: '03', title: 'Skill 管理', sub: '',
      what: `
列出两处 Skill：用户目录 \`~/.dsh/skills\` 里的（套件随安装放进去的 \`control-deck-authoring\`、\`desktop-pet\`、\`skin-studio\`）和本体源码自带的 12 个开发规程。每行显示来源与描述。下方「Skill 市场」按关键词搜 GitHub 仓库，下载解压到用户目录。
`,
      did: `
- 三个 dsh 内 Skill 都是原创：皮肤工坊（让模型做皮肤）、控制甲板编写（让模型帮你写提示词 / 正则 / 世界书，或迁移 SillyTavern 资产）、桌宠制作（从人设到素材清单）。
- 仓库另有 7 个给 Claude Code 用的工程 Skill，不出现在这一页，见「Skill 清单」。
`},
    { type: 'figure', img: '04-launcher-sessions', fig: '04', title: '对话管理', sub: '',
      what: `
\`~/.dsh/sessions\` 下的全部会话按工作目录分组（最新在前，组头显示数量与最近时间），可按工作区 / 会话 id 过滤，「打开会话文件夹」直达资源管理器，展开一组能对单个会话「导出 ZIP」（需要 dsh 在线）。组名前的「≈」表示目录是尽力推断的。
`,
      did: `
- 这一页是只读的：没有删除按钮，删会话请在资源管理器里做。
- 截图里 \`tmp\\2026…\` 那些目录是「临时对话」插件为每次临时会话建的草稿目录。
`},
    { type: 'figure', img: '05-launcher-storage', fig: '05', title: '存储空间', sub: '',
      what: `
上表统计 9 个关键目录（本体仓库、\`~/.dsh\`、会话、存储、profile、插件、用户 Skill、启动器、node_modules）的占用，每行可以直接打开。下面的配置备份把套件在 \`~/.dsh\` 下的配置打成一个 JSON：\`settings.yaml\`、控制甲板与预设、联网搜索、安全规则、模型参数、记忆库、profile 补丁、Skill、hooks、前端皮肤；「预览内容」先列出文件再确认；「导入恢复」前会把现有文件另存一份。
`,
      did: `
- 备份不包含密钥文件和会话日志；恢复只接受白名单内的文件，越界或超限的包会被拒绝，并标出哪些文件需要重启 dsh 才生效。
- 恢复的记忆库在 dsh 运行中也会被合并进来。
`},
    { type: 'figure', img: '06-launcher-update', fig: '06', title: '更新推送', sub: '',
      what: `
左上「版本」卡对比本地本体与上游最新版本（有新版本时标出），三种更新方式：更新本体（git pull + 构建）、升级到上游最新 tag、升级到指定 tag。右上「插件」卡更新 profile 里的全部插件。中间的启动器自检八项：Node 版本、pnpm、端口、本体检出、插件 peer 链接、配置文件、内置 CLI 是否已构建、web profile 已注册的插件。下方实时滚动更新输出。页面上还有一张卡，用来修复 0.1.5 本体拒绝打开的旧会话日志：先预览，停掉 dsh 后再修复。
`,
      did: `
- 「升级到 tag」可以把本体整体换成任意上游版本并重新构建，套件功能不受影响。
- 自检的插件名单读自 \`plugins/\` 目录，新插件落地即被检查；过旧的社区插件会被点名。
`},
    { type: 'figure', img: '07-launcher-tokens', fig: '07', title: 'Tokens 统计', sub: '',
      what: `
Claude Code 风格的用量总览：Overview / Models 两个分页，All / 30d / 7d 时间窗；八张卡片——会话数、消息数、总 tokens、活跃天数、当前与最长连续天数、最忙的一天、最常用模型；26 周热力图；下方按日明细列出每天的调用数、缓存命中 / 未命中、输出与费用。
`,
      did: `
- 数据来自费用插件的账本，启动器只读不另记一份。
- 本地模型免费：回环地址的路由只有命名了付费厂商且带该厂商凭据时才计费。
- Models 分页按模型拆分 tokens 与费用。
`},
    { type: 'figure', img: '08-launcher-control-deck', fig: '08', title: '控制甲板', sub: '',
      what: `
参照 SillyTavern 做的提示词工程台。顶栏：预设（保存 / 载入 / 删除）和导入 / 导出（整份甲板，或 SillyTavern 的世界书 / 正则脚本 / 提示词预设 JSON）。七个分页：提示词（多条分级注入，支持 \`{{date}}\`、\`{{model}}\`、\`{{random:a,b}}\` 等宏，可设间隔）、正则脚本（作用于用户输入 / 世界书 / AI 输出显示）、世界书（关键词命中就注入设定，全套 SillyTavern 字段）、采样与上下文（温度 / 最大输出 / 停止词总开关；工具禁用清单）、联网搜索、安全规则、上手指南。页底一个「保存并热载」写全部分页。
`,
      did: `
- 注入不改写你的原话：提示词与命中的世界书作为单独一条上下文放在你的消息之后。
- 字段与 SillyTavern 同名同义，ST 用户不用重新学；ST 的世界书、正则、提示词预设 JSON 可直接导入导出。
- 写坏的正则不会拖垮 dsh：规则在独立线程里跑，超时的会被停用并在状态行点名。
- 正在编辑的内容不会被预设加载或导入的晚回复覆盖；保存失败保留草稿。
`,
      beyond: `
联网搜索与安全规则分页的内容见 dsh 设置里的对应截图（两边写的是同一份配置）。字段速查在 \`control-deck-authoring\` Skill 里，可以直接让模型替你写。
`},
    { type: 'figure', img: '09-launcher-model-params', fig: '09', title: '模型参数', sub: '',
      what: `
给任意模型设上下文上限（\`contextWindow\`）与最大输出（\`maxTokens\`），能改档位的路由还可设思考档位；本地模型（LM Studio / Ollama）可以探测后端、自动写入推荐档位，之后直接在 dsh 的模型选择器里选。工具栏：「探测 LM Studio / Ollama」「刷新」「自动为未声明档位的本地模型写入推荐档位」、按模型 id 过滤。截图时 dsh 未运行，列表为空。
`,
      did: `
- 在线时列出每条路由（DeepSeek 官方、OpenRouter、本地）的每个模型及当前生效值，每行单独保存。
- 本地模型按家族写入档位；只有开 / 关思考的模型（如 Qwen3）选 Off 时会附加 \`/no_think\`。上下文上限也在这里改：dsh 默认 262,144，本地服务往往只装了 8k，不改就会在错的位置压缩。
- OpenRouter 路由每行可以加 / 删 \`:online\` 联网变体。
- 保存一行不会丢其它行的草稿。
`},
    { type: 'figure', img: '10-launcher-credentials', fig: '10', title: '凭据中心', sub: '',
      what: `
在线时列出每一个 API 凭据引用（如 \`OPENROUTER_API_KEY\`）：是否已配置、存在哪里、绑定在哪些功能上（模型路由、多媒体、联网搜索、桌宠、记忆向量）、别名与备注，以及设置 / 替换 / 删除、备用密钥；可添加新引用、按「只看有绑定的」过滤。下方两张卡：「模型清单」卡上的「刷新模型清单」把 OpenRouter 最新目录同步进路由；「dsh 默认模型」卡先选路由再选模型。dsh 设置里有同名分区，两边写的是同一处。
`,
      did: `
- 引用列表自动汇总，新路由或新插件出现即自动列出；密钥值从不回显。
- 备用密钥：一个引用可以存多把，点一下切换，被换下的自动留作备用。
- 默认模型写入前校验路由清单，避免写进一个不存在的 id。
`},
    { type: 'figure', img: '11-launcher-memory', fig: '11', title: '记忆与上下文', sub: '',
      what: `
三个分页。会话上下文：每个会话（打开与否都行）的上下文占用与压缩阈值、当前生效的压缩摘要（可编辑）、压缩历史、「立即压缩」「存入长期记忆」。长期记忆：摘要 / 事实 / 笔记条目，搜索、钉住、改范围、编辑、删除、导入导出。设置：自动存摘要、自动抽取事实、注入方式、每次注入条数、是否给模型记忆工具、可选向量召回，以及召回测试。截图时 dsh 未运行。
`,
      did: `
本体压缩后的摘要以前只能看不能改，会话之间也没有记忆；这一页补上两件事：改摘要、跨会话记忆。详见「上下文与记忆管理」。
`},
    { type: 'figure', img: '12-launcher-skins', fig: '12', title: '外观皮肤', sub: '',
      what: `
三块：界面设置（\`default\` 皮肤的明 / 暗 / 跟随系统）；启动器皮肤（\`cyberpunk-2077\` / \`default\` / \`night-city-holo\`，切换即时生效，可导入 CSS）；前端（DSH WEB UI）皮肤（「(无)」恢复原生 / \`cyberpunk-2077\` / \`night-city-holo\`，刷新 dsh 页面生效，可导入，「获取社区皮肤」打开市场）。截图里两端都用夜之城全息皮肤。
`,
      did: `
- 启动器皮肤与 dsh 前端皮肤分开管理，都能导入自制 CSS。
- 社区皮肤市场把 npm 上的皮肤包转换成本地 CSS，之后像自制皮肤一样切换、删除，不会混进插件系统。
- 内置 \`night-city-holo\`（石墨底、全息青细线、2077 金激活态）与 \`cyberpunk-2077\`（霓虹黄 × 电青）。
`},
    { type: 'figure', img: '13-launcher-logs', fig: '13', title: '内部日志', sub: '',
      what: `
四个来源：启动器日志、dsh 输出、本体更新、插件更新；只看 ERROR、关键词过滤、复制、下载完整文件。
`,
      did: `
更新、皮肤转换、备份恢复的输出都在这里；日志里从不出现访问令牌。
`},
  ],
};
