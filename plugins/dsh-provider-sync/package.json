{
  "name": "dsh-provider-sync",
  "version": "0.2.0",
  "description": "Keeps an OpenRouter route's model list in DeepSeek Harness current: pulls the public catalog on a schedule (and on demand), adds new models, refreshes names / context windows / output limits, and declares the reasoning-effort levels for every model that supports them — so the reasoning picker works instead of answering 'unsupported'.",
  "type": "module",
  "main": "lib/index.js",
  "exports": {
    ".": {
      "default": "./lib/index.js"
    },
    "./package.json": "./package.json",
    "./client": {
      "default": "./lib/client.js"
    }
  },
  "files": [
    "lib",
    "cordis.patch.yml",
    "README.md"
  ],
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    },
    "client": {
      "platform": "web"
    }
  },
  "license": "MIT",
  "peerDependencies": {
    "@deepseek-ai/dsh-home-paths": "*",
    "@dsh-suite/kit": "*"
  }
}
