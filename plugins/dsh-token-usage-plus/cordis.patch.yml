# dsh-token-usage-plus bundle patch: one host row (the row name is the package
# name, resolved from the profile's node_modules); the web app loads ./client.
- insert:
    - id: dsh-token-usage-plus
      name: dsh-token-usage-plus
